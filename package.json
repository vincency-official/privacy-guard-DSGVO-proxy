{
  "name": "privacy-guard-proxy",
  "version": "0.1.0",
  "description": "Lokaler DSGVO-Reverse-Proxy, der PII/Secrets in KI-Anfragen tokenisiert und Antworten re-identifiziert.",
  "type": "module",
  "bin": {
    "privacy-guard": "dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
